# جاهزية إطلاق نَسَق

تاريخ التقييم: 20 أغسطس 2026

## حالة النسخة

الحالة الحالية: **Release Candidate قابلة للنشر التقني**. لا تُفتح لبيانات حساسة فعلية إلا بعد إتمام بوابات التشغيل الخارجية أدناه.

## ما تم إقفاله

- Next.js 16.3.1 وبناء إنتاجي ناجح.
- 7 اختبارات عقود ناجحة دون فشل.
- `npm audit --omit=dev`: صفر ثغرات معروفة.
- إزالة قارئ SheetJS القديم وحزم PDF غير المستخدمة؛ قارئ XLSX/CSV الحالي محدود بـ10 ميغابايت و10,000 سجل.
- Supabase مصدر الحقيقة الوحيد في الإنتاج؛ لا نجاح وهمي ولا بيانات تجريبية بديلة عند الخطأ.
- RLS وعزل المخيمات وعدم تخزين كلمات المرور في مخطط `public`.
- حذف متسلسل للمخيم وبياناته مع تأكيد صريح بمعرّف المخيم.
- ترقية حماية المسارات من `middleware` إلى `proxy` وفق Next.js 16.
- ترويسات أمان HTTP مطبقة.
- لا تمرير أفقي في صفحة الدخول على 320 و375 و414 و768 بكسل، وأهداف اللمس لا تقل عن 44 بكسل.
- وثيقة SRS عربية رسمية وتقرير إصدار ودليل نشر محدث.

## بوابات إلزامية قبل استقبال بيانات حقيقية

1. تدوير مفتاح Supabase السري الذي سبق مشاركته، ثم تحديثه في الاستضافة فقط.
2. تفعيل حماية كلمات المرور المسرّبة في Supabase Auth.
3. نشر رابط HTTPS نهائي وضبط `NEXT_PUBLIC_APP_URL`.
4. تنفيذ اختبار E2E كامل بحساب مشرف عام وحساب مدير مخيم من بيئة نشر متصلة.
5. تفعيل النسخ الاحتياطي واختبار الاستعادة وتوثيق المسؤول والزمن المقبول للاستعادة.
6. اعتماد سياسة الخصوصية وشروط الاستخدام وفترة الاحتفاظ والصلاحيات التشغيلية.
7. إعداد مراقبة أخطاء وتوافر ووسيلة دعم للمستخدمين.

## ملاحظات غير حاجبة حاليًا

- Supabase Performance Advisor قد يعرض فهارس غير مستخدمة لأن جداول الأسر والترشيحات ما زالت فارغة؛ لا تُحذف قبل توفر بيانات تشغيلية وخطة استعلامات فعلية.
- فحص SRS البنيوي وإتاحة الوصول نجحا، لكن التحويل المرئي التلقائي عبر LibreOffice تعذر لعدم تثبيته على الجهاز. يجب فتح النسخة النهائية في Word ومراجعة الصفحات قبل إرسالها خارج المؤسسة.
