# دليل النشر الإنتاجي

## 1. متطلبات المنصة

- Node.js 20.9 أو أحدث.
- استضافة تدعم Next.js 16 App Router وNode runtime؛ Vercel مناسب افتراضيًا.
- نطاق رسمي وشهادة HTTPS.
- مشروع Supabase إنتاجي مستقل.

## 2. تهيئة Supabase

للتثبيت الجديد شغّل `supabase_schema.sql` كاملًا على قاعدة فارغة. عند ترقية قاعدة قائمة استخدم migration مراجعة من `database/production_hardening.sql` بدل نسخ الأوامر عشوائيًا.

تحقق بعد التطبيق من الآتي:

- RLS مفعّل على جميع جداول `public`.
- المستخدمون مرتبطون بـ`auth.users` ولا يوجد عمود كلمة مرور عام.
- مفاتيح `camp_id` الخارجية تستخدم `ON DELETE CASCADE`.
- وظائف الإدارة مقيدة بالأدوار المناسبة وليست واجهات عامة مفتوحة.
- Realtime محصور بالجداول المطلوبة فقط.

من لوحة Supabase Auth فعّل حماية كلمات المرور المسرّبة وسياسة قوة كلمة المرور. دوّر أي مفتاح سري سبق عرضه في محادثة أو لقطة شاشة قبل وضع النظام على الإنترنت.

## 3. متغيرات البيئة

اضبط القيم التالية في إعدادات الاستضافة لكل بيئة:

```env
NEXT_PUBLIC_SUPABASE_URL=https://PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
SUPABASE_SECRET_KEY=sb_secret_...
JWT_SECRET=قيمة-عشوائية-لا-تقل-عن-32-محرفًا
NEXT_PUBLIC_APP_URL=https://your-domain.example
```

لا تضع `SUPABASE_SECRET_KEY` في متغير يبدأ بـ`NEXT_PUBLIC_`، ولا ترفع `.env.local` إلى GitHub.

## 4. البناء والنشر

```bash
npm ci
npm run verify
npm audit --omit=dev
npm start
```

على Vercel استخدم أمر البناء `npm run build`. لا يحتاج المشروع أمرًا خاصًا لـTurbopack في Next.js 16.

## 5. اختبار ما بعد النشر

1. افتح رابط HTTPS النهائي وتحقق من صفحة الدخول على الهاتف والحاسوب.
2. اختبر حساب المشرف العام ثم حساب مدير مخيم.
3. أنشئ مخيمًا تجريبيًا بكلمة مرور مطابقة للسياسة.
4. أضف أسرة وترشيحًا واستورد ملف XLSX أو CSV صغيرًا.
5. صدّر الكشف وراجع الاسم والشعار والمسؤول والطباعة.
6. اختبر طلب تجديد الاشتراك واعتماده.
7. احذف المخيم التجريبي بعد كتابة معرّفه، ثم حدّث الصفحة وتأكد من عدم عودته.
8. راجع سجلات الاستضافة وSupabase دون تسجيل أسرار أو بيانات شخصية زائدة.

## 6. الاستمرارية والحوكمة

- فعّل النسخ الاحتياطي المناسب للخطة واختبر الاستعادة دوريًا.
- عيّن مالكًا للمراقبة والاستجابة للحوادث والدعم.
- أنشئ حساب مشرف عام احتياطيًا وآمنًا، وفعّل MFA للحسابات الإدارية عندما تدعمه سياسة الجهة.
- اعتمد سياسة خصوصية وشروط استخدام وفترة احتفاظ وآلية تصحيح/حذف للبيانات.
- لا تستقبل بيانات حساسة فعلية قبل نجاح رحلة المستخدم الكاملة واختبار الاستعادة على بيئة الإنتاج.
