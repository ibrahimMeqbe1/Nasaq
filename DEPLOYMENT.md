# دليل الإطلاق الإنتاجي

## 1. قاعدة البيانات

1. أنشئ مشروع Supabase مخصصًا للإنتاج.
2. افتح SQL Editor وشغّل `supabase_schema.sql` كاملًا.
3. أنشئ حساب المشرف العام داخل Supabase Auth، ثم أضف صفًا مطابقًا في
   `public.users` بنفس UUID وبالدور `superadmin`.
4. شغّل `node scripts/migratePasswords.mjs` مرة واحدة فقط إذا كانت لديك
   حسابات قديمة، ثم تحقق أن كل الحسابات تستطيع الدخول عبر Supabase Auth.

## 2. متغيرات الإنتاج

انسخ أسماء المتغيرات من `.env.example` إلى منصة الاستضافة. لا ترفع ملف
`.env` إلى GitHub. أنشئ `JWT_SECRET` عشوائيًا بطول 32 بايت على الأقل، مثل:

```bash
openssl rand -base64 48
```

مفتاح `SUPABASE_SECRET_KEY` سري ويجب أن يبقى في الخادم فقط. استخدم مفتاح
Supabase القابل للنشر في `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.

## 3. النشر

يمكن نشر المشروع على Vercel مباشرة من GitHub. إعدادات البناء:

- Build command: `npm run build`
- Start command (للاستضافة الذاتية): `npm start`
- Node.js: إصدار LTS مدعوم من Next.js 15

بعد ضبط المتغيرات، نفّذ نشرًا إنتاجيًا ثم اختبر الدخول بحساب مشرف عام وحساب
مخيم عادي.

## 4. فحص ما بعد النشر

- تأكد أن المستخدم غير المسجل يُحوّل إلى `/login`.
- تأكد أن مدير مخيم لا يستطيع رؤية أو تعديل بيانات مخيم آخر.
- اختبر إنشاء مخيم وحساب مدير من لوحة المشرف العام.
- اختبر إضافة/تعديل/حذف عائلة وترشيح، والتصدير والطباعة على الهاتف والحاسوب.
- راجع Supabase Database Advisors وفعّل النسخ الاحتياطية المناسبة للخطة.
- اربط نطاقًا مخصصًا، وفعّل مراقبة الأخطاء، وسجّل جهة اتصال للدعم والخصوصية.

## ملاحظة خصوصية

النظام يتعامل مع بيانات شخصية وحساسة. يجب تحديد سياسة احتفاظ، صلاحيات موظفين،
نسخ احتياطي، وآلية حذف/تصحيح البيانات بما يتوافق مع قوانين الجهات المستخدمة.
لا تعتبر بيانات `localStorage` تشفيرًا آمنًا ضد مستخدم الجهاز؛ استخدم أجهزة موثوقة
وامسح بيانات المتصفح بعد انتهاء جلسات العمل المشتركة.
